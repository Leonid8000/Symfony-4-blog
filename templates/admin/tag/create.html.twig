{% extends 'base.html.twig' %}

{% block title %}Tag Create!{% endblock %}

{% block body %}
    {{ include('admin/layouts/header.html.twig') }}

    <div class="container-fluid">
        <div class="row">
            <div class="col-3">
                {{ include('admin/layouts/sidebar.html.twig') }}
            </div>

            <div class="main">
                <h1 class="text-center anton-font text-dark">Create tag</h1>
            </div>

        </div>
    </div>

    <div class="d-flex justify-content-center">
        {{ form_start(tagForm) }}
        {{ form_widget(tagForm) }}
        {{ form_end(tagForm) }}
    </div>

    {{ include('admin/layouts/footer.html.twig') }}
{% endblock %}